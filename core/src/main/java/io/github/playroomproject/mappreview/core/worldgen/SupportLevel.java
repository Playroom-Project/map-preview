package io.github.playroomproject.mappreview.core.worldgen;

/** Accuracy is explicit so estimated output cannot silently become verified output. */
public enum SupportLevel { UNSUPPORTED, ESTIMATED, SAMPLED, VERIFIED }
