# Minecraft 1.20.1 adapter

The implementation now lives in `versions/shared` and `versions/legacy`, selected by `gradle/targets.json`. This directory is retained as a pointer for earlier links. Mapping namespace: Yarn. Game Java baseline: 17.

See `docs/fabric-1.20.x.md` for installation and controls and `docs/validation-1.20.x.md` for actual validation evidence. Loader bindings live in `platforms/fabric`; shared modules contain no Minecraft types.
