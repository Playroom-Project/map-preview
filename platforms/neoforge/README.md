# NeoForge binding

Status: planned. Select a supported Minecraft/NeoForge pair and its required JDK when implementing this adapter.

Provide the same base behavior through native NeoForge hooks. Do not reuse Forge metadata or assume identical version support. Shared base libraries remain Java 17-compatible; target-specific code can use the required newer JDK.
